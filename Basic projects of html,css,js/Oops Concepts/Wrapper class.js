let movie="ps1";
console.log(movie.length);
console.log(movie.toUpperCase()); 
console.log(movie.toLowerCase());
// Wrapper classes are used to convert primitive data types into objects. Here the string is a primitive data type & .length is a method of string object. So we can use wrapper classes to convert primitive data types into objects, and then we can access the methods of that object. Its a temporary conversion.
